---
import BaseLayout from "../layouts/BaseLayout.astro";

const homepageTitle: string = "Page Not Found";
---

<BaseLayout pageTitle={homepageTitle}>
    <h1>{homepageTitle}</h1>
    <p>This page cannot be found.</p>
</BaseLayout>
