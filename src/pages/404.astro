---
import DefaultLayout from "@layouts/DefaultLayout.astro";

const homepageTitle: string = "Error 404 | Page Not Found";
---

<DefaultLayout pageTitle={homepageTitle}>
    <h1>{homepageTitle}</h1>
    <p>This page cannot be found.</p>
</DefaultLayout>
